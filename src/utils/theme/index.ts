import {
  Consumer as AppThemeConsumer,
  Context as AppThemeContext,
} from "./ThemeProvider";
import AppThemeProvider from "./ThemeProvider";
export { AppThemeConsumer, AppThemeContext };
export default AppThemeProvider;
